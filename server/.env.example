# Copy to .env and fill in. Never commit .env.
# Neon (free tier) connection string — https://neon.tech
DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/asahi?sslmode=require

# Optional separate database for integration tests.
DATABASE_TEST_URL=

NODE_ENV=development
PORT=4000
